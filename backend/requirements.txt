# =============================================================
# InsureCompare.at backend - production dependencies
# Pin exact versions for reproducibility.
# =============================================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Pydantic v2 ---
pydantic==2.12.5
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Database ---
SQLAlchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3
pgvector==0.3.4

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- HTTP / utils ---
httpx==0.28.1
python-dotenv==1.0.1
structlog==24.4.0
orjson==3.10.7

# --- File processing ---
Pillow==10.4.0
pdfminer.six==20240706
pytesseract==0.3.13
PyPDF2==3.0.1
# Used by both the application PDF-to-OCR fallback and the reproducible
# real-document OCR evaluation.
pymupdf==1.28.2

# --- AI Policy Advisor (LLM) ---
# Official Google Gen AI SDK, used by app/llm/gemini_provider.py when
# LLM_PROVIDER=gemini. Tests/CI use LLM_PROVIDER=mock (app/llm/mock_provider.py)
# and never import this package's client at runtime for that path.
google-genai==2.20.0

# --- NLP / ML ---
spacy==3.7.6
transformers==4.44.2
torch==2.4.1
scikit-learn==1.5.2
numpy==1.26.4
shap==0.46.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
faker==30.1.0
aiosqlite==0.20.0
